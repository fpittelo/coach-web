"""Swiss minimalist design tokens — EPFL-inspired.

Single source of truth for all design tokens used across coach-web.
Colors are derived from the EPFL Elements design system (https://www.epfl.ch).
"""

# Color tokens (EPFL-inspired)
BG_PRIMARY: str = "#FFFFFF"
"""Page background — pure white."""

BG_SECONDARY: str = "#F5F5F5"
"""Card and section backgrounds — soft gray."""

TEXT_PRIMARY: str = "#212121"
"""Body text and headings — EPFL --dark."""

TEXT_SECONDARY: str = "#707070"
"""Labels, captions, metadata — EPFL gray-600."""

ACCENT: str = "#FF0000"
"""Primary actions and key data points — EPFL red."""

ACCENT_DARK: str = "#B51F1F"
"""Hover, active, and focus states — EPFL focus red."""

BORDER: str = "#D5D5D5"
"""Hairline borders and dividers — EPFL gray-200."""

# Typography
FONT_FAMILY: str = "Inter"
"""Primary typeface — bundled locally (Swiss nLPD compliant)."""

# Layout
BORDER_RADIUS: str = "2px"
"""Signature EPFL crisp corner radius."""
